<%- include('./partials/head') %> 
<h1>Tu Carrito</h1>
<div class="container sm-6">
    <table class="table">
        <thead>
          <tr>
            <th scope="col">#</th>
            <th scope="col">Title</th>
            <th scope="col">Price</th>
            <th scope="col">URL</th>
          </tr>
        </thead>
        <% products.forEach(product =>{ %>
            <tbody>
                <tr>
                  <th scope="row"><%=product.id%></th>
                  <td><%=product.nombre %></td>
                  <td><%=product.descripcion %></td>
                  <td><%=product.precio %></td>
                  <td><button onclick = "agregar('<%=product.id%>')">"TODO" Eliminar</td>
                </tr>
            </tbody>
        <% }) %> 
     </table>
        <!-- <form action = "/" method = "post">
            <button type="submit" class="btn btn-dark">Volver al Post</button>
        <form> -->
    </div>
    </body>
    </html>